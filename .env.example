# Example env defaults for local development
# Enable the visits counter UI
VITE_SHOW_VISITS=true

# Enable fuzzy Related Q&A fallback (default: false)
# Set to 'true' to allow fuzzy matches to show Related Q&A buttons when no explicit assignment exists
VITE_RELATED_QA_FUZZY=false
